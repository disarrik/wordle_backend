spring:
  session:
    store-type: redis
  redis:
    host: redis # set to 'redis' when running as a container.
    database: 0
    port: 6379
reset-word-task:
  delay: 300000
  fileLocation: "/wordbank.txt"
