spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/postgres # 'postgres' if running with compose
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
reset-word-task:
  delay: 300000
  fileLocation: "/wordbank.txt"
