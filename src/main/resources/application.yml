spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
reset-word-task:
  delay: 300000
  fileLocation: "/wordbank.txt"
