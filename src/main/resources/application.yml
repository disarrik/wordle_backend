spring:
  session:
    store-type: redis
  redis:
    host: localhost # set to 'redis' when running as a container.
    database: 0
    port: 6379
reset-word-task:
  delay: 10000
  fileLocation: "src/main/resources/wordbank.txt"
